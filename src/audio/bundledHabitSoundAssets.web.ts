/** Web preview has no bundled binary audio in this build. */
export const TIMER_KEEPALIVE_SOUND_MODULE = 0;

export function getBundledHabitSoundModule(_trackId: string): number | undefined {
  return undefined;
}
